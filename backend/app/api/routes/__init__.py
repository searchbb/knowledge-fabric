"""Phase 2 route scaffolds.

Current runtime routing still enters through legacy blueprints under
`backend/app/api/*.py`. New route modules live here first so logic can be
gradually migrated without breaking existing entrypoints.
"""
